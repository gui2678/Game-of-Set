// might change to make all info string and use indexes to access each piece of information

class Card {
    constructor(number, color, shape, attr, image){
        this.number = number;
        this.color = color;
        this.shape = shape;
        this.attr = attr;
        this.image = image;
    }
}

export default Card;